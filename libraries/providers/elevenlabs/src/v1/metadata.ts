import { ProviderMetadata } from '@gitroom/provider-kernel';

export const metadata: ProviderMetadata = {
  id: 'elevenlabs',
  displayName: 'elevenlabs',
  kind: 'action',
  domains: ['media'],
  mediaCategories: ['text-to-speech'],
  hasModelList: false,
};
