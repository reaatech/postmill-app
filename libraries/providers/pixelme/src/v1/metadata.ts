import { ProviderMetadata } from '@gitroom/provider-kernel';

export const metadata: ProviderMetadata = {
  id: 'pixelme',
  displayName: 'pixelme',
  kind: 'action',
  domains: ['media'],
  hasModelList: false,
};
