import { ProviderMetadata } from '@gitroom/provider-kernel';

export const metadata: ProviderMetadata = {
  id: 'suno',
  displayName: 'suno',
  kind: 'action',
  domains: ['media'],
  mediaCategories: ['text-to-music'],
  hasModelList: false,
};
