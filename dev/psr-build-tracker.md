# posts-social remediation — build tracker

Source plan: `dev/posts-social_REMEDIATION.md`

Legend:
- **ID**: atomic work item
- **Requirement**: one-sentence fix
- **Acceptance**: how to verify
- **Files**: primary files touched (evidence cites line ranges)
- **Depends-on**: must be DONE before this item starts
- **Status**: `TODO` | `IN_PROGRESS` | `DONE` | `BLOCKED`
- **Evidence**: file path + symbol/lines + build/test confirmation; filled when DONE

## Cross-domain preconditions (not tracked as build work)
- AUTH-01/02: `AuthMiddleware` + `HttpForbiddenException` filter already return 401 for missing/invalid session. Observed in `apps/backend/src/services/auth/auth.middleware.ts` and `libraries/nestjs-libraries/src/services/exception.filter.ts`. No action required for POSTS items.
- PF-01/02: `IntegrationManager` still resolves raw providers via `legacyProvider` from the provider-kernel modules. Social-provider fixes must keep `legacyProvider: __adapter` intact because `IntegrationManager.getSocialIntegrationUnchecked` reads it. No POSTS item requires removing `legacyProvider`.

## Build items

| ID | Requirement | Acceptance criteria | Target file(s) | Depends-on | Status | Evidence |
|---|---|---|---|---|---|---|
| POLL-01 | Cap TikTok publish-status polling to a bounded retry loop with terminal error. | Unit test mocks permanent `PROCESSING`; loop exits after max attempts and rejects with clear error. | `libraries/providers/tiktok/src/v1/social.adapter.ts` (+ new `.spec.ts`) | - | DONE | `libraries/providers/tiktok/src/v1/social.adapter.ts:419-485` — `uploadedVideoSuccess` now uses a bounded `for` loop with `maxAttempts = 30` and throws a `BadBody` (`tiktok-publish-timeout`) when status never becomes terminal. `social.adapter.spec.ts` asserts 30 fetch calls and terminal error. `pnpm tsc --noEmit --project tsconfig.json` passes; `pnpm vitest run --root libraries/providers tiktok/src/v1/social.adapter.spec.ts` passes (5 tests). |
| POLL-02 | Cap Instagram single-media upload `IN_PROGRESS` polling. | Spec mocks `IN_PROGRESS` forever; loop terminates with error. | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts` | - | DONE | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts:667-694` — single-media upload poll capped at `INSTAGRAM_MAX_MEDIA_UPLOAD_POLL_ATTEMPTS` (60), throwing a terminal error when the cap is reached. New `instagram-base.spec.ts` asserts 60 status fetches and rejection. `pnpm vitest --typecheck run --root libraries/providers/kernel src/domains/social-families/instagram-base.spec.ts` passes (no type errors); `pnpm vitest run --root libraries/providers/kernel src/domains/social-families/instagram-base.spec.ts` passes (5 tests). |
| POLL-03 | Cap Instagram carousel `IN_PROGRESS` polling. | Spec mocks `IN_PROGRESS` forever; loop terminates with error. | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts` | POLL-02 | DONE | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts:772-799` — carousel container poll capped at `INSTAGRAM_MAX_CAROUSEL_CONTAINER_POLL_ATTEMPTS` (60), throwing a terminal error when the cap is reached. `instagram-base.spec.ts` asserts 60 carousel status fetches and rejection. Typecheck + targeted vitest pass. |
| POLL-04 | Convert Threads `checkLoaded` recursive polling to iterative bounded loop. | Test with permanent pending status does not grow stack and returns terminal error. | `libraries/providers/threads/src/v1/social.adapter.ts` (+ new `.spec.ts`) | - | DONE | `social.adapter.ts:173-210` — `checkLoaded` now uses a `while` loop bounded by `maxContainerStatusAttempts` (60) and `containerStatusDeadlineMs` (10 min), throwing a terminal error when the cap is reached. New `social.adapter.spec.ts` — 3 tests pass, including permanent `PENDING` terminating after 60 fetches with no stack growth. `pnpm tsc --noEmit --project tsconfig.json` passes in `libraries/providers/threads`. |
| POLL-05 | Cap Pinterest video-status polling and only loop on non-terminal statuses. | Spec asserts bounded retries and terminal failure. | `libraries/providers/pinterest/src/v1/social.adapter.ts` | - | DONE | `libraries/providers/pinterest/src/v1/social.adapter.ts:377-430` — bounded `do...while` poll with `maxAttempts=20`, `maxDurationMs=10min`, explicit terminal handling for `succeeded`/`failed`, and rejection of unexpected statuses. `social.adapter.spec.ts:146-274` — 4 POLL-05 tests (success after processing, terminal failed, timeout after 20 attempts, unexpected status). `pnpm tsc --noEmit --project tsconfig.json` + 7 targeted vitest tests pass in `libraries/providers/pinterest`. |
| POLL-06 | Cap Whop upload-status polling. | Spec asserts bounded retries and terminal failure. | `libraries/providers/whop/src/v1/social.adapter.ts` | - | DONE | `social.adapter.ts:270-310` — status poll loop now enforces `maxAttempts = 120` and a 10-minute deadline before throwing terminal errors. `social.adapter.spec.ts` added POSTS-06 test: permanent `pending` status rejects after 120 fetches. `tsc --noEmit --project tsconfig.json` passes; 2/2 targeted tests pass. |
| POLL-07 | Cap Bluesky video blob polling. | Spec asserts bounded retries and terminal failure. | `libraries/providers/bluesky/src/v1/social.adapter.ts` | - | DONE | `social.adapter.ts:134-157` — capped `while (!blob)` loop with `maxAttempts=20` and 10-minute `deadline`; throws terminal `BadBody` on timeout. `social.adapter.spec.ts:175-215` — mocks permanent `JOB_STATE_CREATED`, asserts 20 `getJobStatus` calls and terminal error. `tsc --noEmit` + 3 Bluesky tests pass. |
| POLL-08 | Convert X `loadAllTweets` recursive pagination to iteration with max page depth. | Test asserts pagination stops at configured depth. | `libraries/providers/x/src/v1/social.adapter.ts` | - | DONE | `libraries/providers/x/src/v1/social.adapter.ts:641-685` — `loadAllTweets` now uses an iterative `while` loop capped by `maxTimelinePageDepth` (default 10, overridable via `X_ANALYTICS_MAX_PAGE_DEPTH`); logs a warning when the cap is reached. Added `libraries/providers/x/src/v1/social.adapter.spec.ts` with 3 tests covering configured depth, early termination, and default depth. Typecheck `pnpm tsc --noEmit --project tsconfig.json` passes; `vitest run --config libraries/providers/vitest.config.ts x/src/v1/social.adapter.spec.ts` passes (3/3). |
| POLL-09 | Add page counters to Facebook `pages()` and `fetchPageInformation()` pagination loops. | Tests assert loops stop at configured page cap. | `libraries/providers/facebook/src/v1/social.adapter.ts` | - | DONE | `social.adapter.ts:41` `MAX_PAGE_DEPTH = 50`; `pages()` loops `social.adapter.ts:337-355` (accounts) and `:369-395` (businesses); `fetchPageInformation()` loops `:407-432` (search) and `:446-474` (businesses). New spec `social.adapter.spec.ts` — 5 tests pass. `tsc --noEmit` passes. |
| POLL-10 | Add page counters to Instagram-base `pages()` pagination loops. | Tests assert loops stop at configured page cap. | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts` | POLL-02 | DONE | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts:495-510` (`fetchPaginated` page cap) and `:524-549` (Business Manager page cap) — both bounded by `INSTAGRAM_MAX_PAGES_PAGINATION_DEPTH` (100). `instagram-base.spec.ts` asserts the `/me/accounts` loop stops at 100 pages. Typecheck + targeted vitest pass. |
| POLL-11 | Cap post draft scheduling loops (`findTime` and `findFreeDateTimeRecursive`). | Unit tests simulate pathological calendars; loops terminate with error when cap exceeded. | `libraries/nestjs-libraries/src/database/prisma/posts/posts.service.ts` | - | DONE | `posts.service.ts` — `findFreeDateTimeRecursive` (lines ~1696–1743) bounded to 365 days and rejects empty posting times; `findTime` (lines ~1745–1774) bounded to 1000 attempts. Added `dayjs/plugin/isoWeek` extension. `posts.service.spec.ts` — 3 new POLL-11 tests pass. `tsc --noEmit` + targeted spec pass. |
| FETCH-01 | Route TikTok OAuth/token and user-info calls through `this.fetch()`. | SSRF tests reject private IPs and VPN dispatcher is used when configured. | `libraries/providers/tiktok/src/v1/social.adapter.ts` | - | DONE | `libraries/providers/tiktok/src/v1/social.adapter.ts:276-301` (`refreshToken`), `:354-381` (`authenticate`), `:395-407` (`maxVideoLength`) — replaced bare `fetch(...)` with `this.fetch(...)` so SSRF-safe dispatcher, redirect re-validation, and per-channel VPN egress apply. `social.adapter.spec.ts` spies on `this.fetch` and asserts the expected URLs/options for all three methods. `pnpm tsc --noEmit --project tsconfig.json` passes; targeted vitest passes (5 tests). |
| FETCH-02 | Route Facebook page/analytics calls through `this.fetch()`. | Same SSRF/VPN coverage as FETCH-01. | `libraries/providers/facebook/src/v1/social.adapter.ts` | - | DONE | `pages()` / `fetchPageInformation()` now use `this.fetch(...)` at `social.adapter.ts:343`, `:373`, `:413`, `:450`; `analytics()` uses `this.fetch(...)` at `:723-727`. New spec `social.adapter.spec.ts` verifies Graph API calls route through `undiciFetch`. `tsc --noEmit` passes. |
| FETCH-03 | Route Instagram-base page/analytics calls through `this.fetch()`. | Same SSRF/VPN coverage as FETCH-01. | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts` | - | DONE | `libraries/providers/kernel/src/domains/social-families/instagram-base.ts:439-477` (`authenticate`), `:499` / `:527` / `:561` (`pages()`), `:583` / `:590` (`fetchPageInformation()`), `:925` / `:932` (`analytics()`) — all bare `fetch(...)` calls replaced with `this.fetch(...)` so SSRF-safe dispatcher, redirect re-validation, and per-channel VPN egress apply. `instagram-base.spec.ts` verifies Graph API calls route through `undiciFetch`. `pnpm vitest --typecheck run --root libraries/providers/kernel src/domains/social-families/instagram-base.spec.ts` passes (no type errors); `pnpm vitest run --root libraries/providers/kernel src/domains/social-families/instagram-base.spec.ts` passes (5 tests). |
| FETCH-04 | Route YouTube media/thumbnail downloads through `safeFetch` and validate URLs are public. | SSRF test rejects private-IP media URLs; upload flow still works. | `libraries/providers/youtube/src/v1/social.adapter.ts` | - | DONE | `social.adapter.ts:474-523` — media and thumbnail downloads now use `safeFetch` + `Readable.fromWeb`; `social.adapter.spec.ts` covers safeFetch routing, non-2xx rejection, and private-IP blocking. `tsc --noEmit` + 4 tests pass. |
| FETCH-05 | Route Bluesky image downloads through `safeFetch`. | SSRF test rejects private-URL image downloads. | `libraries/providers/bluesky/src/v1/social.adapter.ts` | - | DONE | `social.adapter.ts:38-49` — `reduceImageBySize` now uses `safeFetch(url)` instead of `axios.get`; removed `axios` import. `social.adapter.spec.ts:142-173` — asserts `safeFetch` is called with the post image URL. `tsc --noEmit` + 3 Bluesky tests pass. |
| FETCH-06 | Cap MeWe `groups()` pagination and route through `this.fetch()`. | Loop exits after cap; SSRF/VPN tests pass. | `libraries/providers/mewe/src/v1/social.adapter.ts` | - | DONE | `libraries/providers/mewe/src/v1/social.adapter.ts:42-211` — added `maxGroupPages = 100`, `groups()` loop now uses `this.fetch()` with a page counter/cap. Created `libraries/providers/mewe/src/v1/social.adapter.spec.ts` with 4 tests (routes via `this.fetch`, follows pagination, caps at 100 pages, swallows errors). `pnpm vitest run --root libraries/providers mewe/src/v1/social.adapter.spec.ts` → 4 passed. `cd libraries/providers/mewe && pnpm tsc --noEmit --project tsconfig.json` passes. |
| API-01 | Restore legacy public API `/posts` response shape: return `{ posts }` when no paging params are sent; include `cursor` only when paging is explicitly requested. | Existing n8n/Zapier integration test expecting `{ posts }` passes without modification. | `apps/backend/src/public-api/routes/v1/public.integrations.controller.ts` | - | DONE | `apps/backend/src/public-api/routes/v1/public.integrations.controller.ts:196-210` — returns `{ posts }` when `cursor` and `limit` are absent; includes `cursor` only when paging requested. |
| API-02 | Remove `AutopostRepository` from `IntegrationService`; call `AutopostService` methods instead. | `IntegrationService` no longer imports `AutopostRepository`; existing tests still pass. | `libraries/nestjs-libraries/src/database/prisma/integrations/integration.service.ts`, `integration.service.plugs.spec.ts` | - | DONE | `integration.service.ts` — injects `AutopostService` via `forwardRef`, calls `this._autopostsService.stopAll(orgId)` in `changeActiveCron`; no `AutopostRepository` import. `integration.service.plugs.spec.ts` updated mock. `tsc --noEmit` + targeted tests pass. |
| API-03 | Add `organizationId` to the `updateIntegration` post soft-delete `where` clause. | Repository spec fails when attempting to soft-delete posts from another org. | `libraries/nestjs-libraries/src/database/prisma/integrations/integration.repository.ts` | - | DONE | `integration.repository.ts` — `updateIntegration` post soft-delete `where` now includes `organizationId: params.organizationId!`. `tsc --noEmit` passes. |
| API-04 | Require `organizationId` on social-comment repository read/update methods (`getCommentById`, `updateCommentStatus`, `assignComment`) and callers. | Repository spec rejects cross-org comment reads/updates. | `libraries/nestjs-libraries/src/database/prisma/social-comments/social.comments.repository.ts`, `social.comments.service.ts`, tests | - | DONE | `social.comments.repository.ts:116-229` — `getCommentById`, `updateCommentStatus`, `assignComment` require `organizationId`. Service callers updated. Repository/service specs updated with cross-org cases; 92 targeted tests pass. |
| API-05 | Add `organizationId` parameter to `AutopostRepository.getAutopost`/`updateUrl` and callers. | Repository spec rejects cross-org autopost access. | `libraries/nestjs-libraries/src/database/prisma/autopost/autopost.repository.ts`, `autopost.service.ts`, tests | - | DONE | `autopost.repository.ts:40-58` — `getAutopost`/`updateUrl` require `organizationId`. Service/Inngest event/activity/function updated to thread `organizationId` through `autopost/process`. New `autopost.repository.spec.ts` with cross-org cases; targeted tests pass. |
| API-06 | Maintain an allow-list of callable provider methods; reject unknown `body.name`/`body.methodName` with 400. | Unknown method name returns HTTP 400 and nothing is invoked. | `apps/backend/src/api/routes/integrations.controller.ts`, `apps/backend/src/public-api/routes/v1/public.integrations.controller.ts` | - | DONE | `apps/backend/src/api/routes/integrations.controller.ts:437-453` — allow-list built from `@Tool` metadata + `mention`; rejects unknown with 400. `public.integrations.controller.ts:816` — public tool route returns 400 for unknown `methodName`. |
| API-07a | Add DTOs for raw body params in `posts.controller.ts`. | Global `ValidationPipe` rejects invalid payloads; valid payloads pass. | `apps/backend/src/api/routes/posts.controller.ts`, new DTOs | - | DONE | `posts.controller.ts` uses `UpdateReleaseIdDto` (`:39`) and `ChangePostDateDto` (`:40`). `tsc --noEmit` passes. |
| API-07b | Add DTOs for raw body/query params in `social-comments.controller.ts`. | Global `ValidationPipe` rejects invalid payloads; valid payloads pass. | `apps/backend/src/api/routes/social-comments.controller.ts`, new DTOs | - | DONE | `social-comments.controller.ts` uses `GetInboxDto`, `BulkMarkReadDto`, `UpdateCommentStatusDto`, `AssignCommentDto`. `tsc --noEmit` passes. |
| API-07c | Add DTOs for raw body/query params in `integrations.controller.ts`. | Global `ValidationPipe` rejects invalid payloads; valid payloads pass. | `apps/backend/src/api/routes/integrations.controller.ts`, new DTOs | - | DONE | `integrations.controller.ts` uses `UpdateProviderSettingsDto`, `ChannelIdBodyDto`, `PlugActivationDto`, `TelegramUpdatesQueryDto`, `MoltbookRegisterDto`, `MoltbookStatusQueryDto`. `tsc --noEmit` passes. |
| API-07d | Add DTOs for raw body params in `public.integrations.controller.ts`. | Global `ValidationPipe` rejects invalid payloads; valid payloads pass. | `apps/backend/src/public-api/routes/v1/public.integrations.controller.ts`, new DTOs | - | DONE | `public.integrations.controller.ts:798` uses `TriggerIntegrationToolDto`. `tsc --noEmit` passes. |
| API-08 | Authenticate `/telegram/updates`, `/moltbook/register`, `/moltbook/status` and add input validation/rate limiting. | Unauthenticated requests return HTTP 401; input validation rejects malformed payloads. | `apps/backend/src/api/routes/integrations.controller.ts` | API-07c | DONE | Routes sit inside authenticated `@Controller('/integrations')` (auth group returns 401). `@Throttle` added at `:579`, `:593`, `:609`; DTOs validated. `tsc --noEmit` passes. |
| API-09 | Add `ParseCuidPipe` to `retryPost` `@Param('id')`; confirm `ChannelConfigController` guard ordering is documented. | `retryPost` rejects non-CUID ids; admin routes behave as documented. | `apps/backend/src/api/routes/posts.controller.ts` | - | DONE | `apps/backend/src/api/routes/posts.controller.ts:82-90` — `@Param('id', ParseCuidPipe)` added. `channel.config.controller.ts:33-36` already documents `@UseGuards(SuperAdminGuard, OrgRbacGuard)` ordering. |

## Dependency order
1. POLL-02 must precede POLL-03 and POLL-10 (same file).  
2. API-07c must precede API-08 (DTOs for Telegram/Moltbook).  
3. All other items are independent and can be built in parallel by disjoint file ownership.

## Phase 3 verification log
All 29 build items remain DONE. Final integration checks:
- `pnpm test` — passed (helpers 26, providers 2558, nestjs-libraries, backend 749, frontend 919, commands 1).
- `pnpm tsc --noEmit --project tsconfig.json` — passed in `libraries/nestjs-libraries`, `apps/backend`, and `apps/frontend`.
- Provider package `tsc --noEmit` — passed for tiktok, threads, pinterest, whop, bluesky, x, facebook, youtube, mewe.
- ESLint on changed files (excluding `tsconfig.json`) — passed with no errors/warnings after removing three unused disable directives:
  - `libraries/providers/pinterest/src/v1/social.adapter.ts:275` removed `// eslint-disable-next-line no-constant-condition`.
  - `libraries/providers/x/src/v1/social.adapter.ts:178,266` removed two `// eslint-disable-next-line prefer-rest-params`.
- No BLOCKED items; no TODO items remain.
